services:
  redis:
    image: redis:latest
    container_name: redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  postgres:
    image: postgres:16
    container_name: postgres
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-codexgraph}
      POSTGRES_USER: ${POSTGRES_USER:-codexgraph}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-codexgraph}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-codexgraph} -d ${POSTGRES_DB:-codexgraph}"]
      interval: 5s
      timeout: 5s
      retries: 5

  codexgraph:
    build:
      context: .
      dockerfile: dockerfile
    container_name: codexgraph
    ports:
      - "8000:8000"
    depends_on:
      redis:
        condition: service_started
      postgres:
        condition: service_healthy
    env_file:
      - .env
    environment:
      APP_NAME: codexgraph
      DATABASE_URL: ${DATABASE_URL:-postgresql+asyncpg://codexgraph:codexgraph@postgres:5432/codexgraph}
      REPO_CONFIG_FILE: "repos.json"
      CODE_GRAPH_BACKGROUND_REFRESH: "true"
      CODE_GRAPH_REFRESH_INTERVAL_HOURS: "12"
      CODE_GRAPH_REBUILD_TTL_HOURS: "336"
      CODE_VERIFICATION_REPAIR_ATTEMPTS: "0"
      CODE_ANALYSIS_TIMEOUT_SECONDS: "8"
      CODE_ANALYSIS_BUDGET_SECONDS: "6"
      CODE_BATCH_TOKEN_LIMIT: "14000"
      CODE_NODE_CHUNK_TOKEN_LIMIT: "4000"
      CODE_ANALYSIS_MAX_NODES_LIGHT: "6"
      CODE_ANALYSIS_MAX_NODES_DEEP: "8"
      HIDE_INTERNAL_LIMITATIONS: "true"
      ENABLE_BENCH_API: "false"
    volumes:
      - ./chroma_graph_db:/app/chroma_graph_db:Z
      - ./arquivos/chat_retrieval_db:/app/arquivos/chat_retrieval_db:Z
      - code_graph_data:/app/code_graph_storage
      - repos_sources_data:/app/repos_sources

volumes:
  redis_data:
  postgres_data:
  code_graph_data:
  repos_sources_data:
